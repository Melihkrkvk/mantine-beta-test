import NavbarSimple from './NavbarSimple';
import { Meta, StoryObj, Preview } from '@storybook/react';

const meta: Meta<typeof NavbarSimple> = {
  component: NavbarSimple,
};
export default meta;

type Story = StoryObj<typeof NavbarSimple>;

export const Primary: Story = {};
